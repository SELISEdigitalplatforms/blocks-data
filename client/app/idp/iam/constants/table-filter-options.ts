/** Faceted filter options (legacy labels; reused from former UILM “translation status” literals). */
export const translationFilterOptions = [
  { value: "No_translation", label: "No translation" },
  { value: "Complete", label: "Complete" },
] as const;
